# Core
mcp>=1.6.0
PySide6>=6.6.0
pydantic>=2.0.0
python-dotenv>=1.0.0
httpx>=0.27.0

# LLM providers
openai>=1.30.0
anthropic>=0.25.0
# ollama — uses the openai-compatible endpoint, no extra package needed
